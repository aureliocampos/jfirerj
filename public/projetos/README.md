# Projetos (PDFs por cliente)

Pasta reservada para os PDFs de projeto de cada caso, referenciados pelos
cards da seção Casos de sucesso (`src/components/Cases.astro`). Nomeie os
arquivos com o mesmo slug usado em `src/data/cases.ts` (ex.: `bluefit.pdf`).

<!-- TODO(cliente): aguardando um PDF por cliente. -->

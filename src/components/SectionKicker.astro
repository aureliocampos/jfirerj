---
interface Props {
  label: string;
  variant?: "default" | "wash";
}

const { label, variant = "default" } = Astro.props;
---

<p class:list={["kicker", { "kicker--wash": variant === "wash" }]}>{label}</p>

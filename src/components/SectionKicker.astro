---
interface Props {
  label: string;
  variant?: "default" | "wash" | "inverse";
}

const { label, variant = "default" } = Astro.props;
---

<p
  class:list={[
    "kicker",
    { "kicker--wash": variant === "wash", "kicker--inverse": variant === "inverse" },
  ]}
>
  {label}
</p>
